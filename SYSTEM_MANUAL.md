# คู่มือการใช้งานและเอกสารระบบ (System Manual)
## Data Center Operation & Energy Inspection System (DCIM Routine Patrol)

ระบบเว็บแอปพลิเคชันสำหรับบริหารจัดการงานเดินตรวจตราอุปกรณ์และวิเคราะห์พลังงานในศูนย์ข้อมูล (Data Center) ออกแบบตามมาตรฐานงานปฏิบัติการอุตสาหกรรม (Enterprise SCADA / DCIM Style) มีโทนสีสุภาพ ชัดเจน สบายสายตา และรองรับการทำงานในจุดอับสัญญาณแบบออฟไลน์

---

## สารบัญ
1. [ภาพรวมของระบบและเป้าหมาย](#1-ภาพรวมของระบบและเป้าหมาย)
2. [แนวทางการออกแบบ (Design System & Theme)](#2-แนวทางการออกแบบ-design-system--theme)
3. [โครงสร้างบัญชีผู้ใช้และระดับสิทธิ์](#3-โครงสร้างบัญชีผู้ใช้และระดับสิทธิ์)
4. [ฟังก์ชันการทำงานหลัก](#4-ฟังก์ชันการทำงานหลัก)
   - [4.1 งานเดินตรวจรอบ (Routine Patrol & Inspection)](#41-งานเดินตรวจรอบ-routine-patrol--inspection)
   - [4.2 ระบบเลือกอุปกรณ์ด่วนและ Checklist รายห้อง](#42-ระบบเลือกอุปกรณ์ด่วนและ-checklist-รายห้อง)
   - [4.3 แดชบอร์ดและบทวิเคราะห์พลังงาน (Energy Analytics)](#43-แดชบอร์ดและบทวิเคราะห์พลังงาน-energy-analytics)
   - [4.4 ประวัติการเดินตรวจย้อนหลัง (Audit Log)](#44-ประวัติการเดินตรวจย้อนหลัง-audit-log)
   - [4.5 การจัดการอุปกรณ์ ห้อง และพิมพ์ป้าย QR Code](#45-การจัดการอุปกรณ์-ห้อง-และพิมพ์ป้าย-qr-code)
   - [4.6 การทำงานแบบออฟไลน์ (Offline Mode & Auto-Sync)](#46-การทำงานแบบออฟไลน์-offline-mode--auto-sync)
5. [ขั้นตอนการปฏิบัติงานของเจ้าหน้าที่ (Standard Operating Procedure)](#5-ขั้นตอนการปฏิบัติงานของเจ้าหน้าที่-standard-operating-procedure)
6. [ขั้นตอนการทำงานของผู้ดูแลระบบ (Admin Operation)](#6-ขั้นตอนการทำงานของผู้ดูแลระบบ-admin-operation)
7. [โครงสร้างทางเทคนิคและสถาปัตยกรรมโปรเจกต์](#7-โครงสร้างทางเทคนิคและสถาปัตยกรรมโปรเจกต์)
8. [รายการ API Endpoints](#8-รายการ-api-endpoints)
9. [การเปิดใช้งานระบบและการติดตั้ง](#9-การเปิดใช้งานระบบและการติดตั้ง)

---

## 1. ภาพรวมของระบบและเป้าหมาย
- **ความจุผู้ใช้งาน:** รองรับผู้ลงทะเบียนในระบบ 60+ คน พร้อมรองรับการใช้งานพร้อมกันสูงสุด 7+ คนในแต่ละกะ (Peak Concurrent Operators)
- **การเก็บบันทึกข้อมูล:**
  - พารามิเตอร์ทางไฟฟ้า: แรงดัน 3 เฟส (V Phase A, B, C), กระแสไฟฟ้า (A Phase A, B, C), กำลังไฟฟ้ารวม (kW), % Load, Battery Voltage
  - พารามิเตอร์สิ่งแวดล้อม: อุณหภูมิลมกลับ (Return Temp), อุณหภูมิลมจ่าย (Supply Temp), ความชื้นสัมพัทธ์ (%RH), ความเร็วพัดลม (EC Fan Speed)
  - ภาพถ่ายข้อบกพร่อง (Defect Evidence): แนบรูปเฉพาะจุดที่พบความผิดปกติ พร้อมระบบบีบอัดภาพอัตโนมัติบนเครื่องเพื่อประหยัดพื้นที่
- **เป้าหมายด้านพลังงาน:** ตรวจจับจุดความร้อนสะสม (Hotspot > 26°C) เพื่อป้องกันระบบหยุดชะงัก และตรวจจับจุดทำความเย็นเกินจำเป็น (Overcooling < 19°C) เพื่อปรับเพิ่ม Setpoint แอร์ ช่วยประหยัดค่าไฟฟ้าของ Data Center

---

## 2. แนวทางการออกแบบ (Design System & Theme)
ระบบได้รับการออกแบบตามแนวทาง **Enterprise SCADA & Industrial Control Interface**:
- **Palette สีสุภาพและเป็นทางการ:** ใช้พื้นหลังสีดำลึกและเทาเข้ม (`slate-950`, `slate-900`) ตัดกับเส้นขอบบางประณีต (`border-slate-800`) เหมาะกับหน้าจอ Control Room และสมาร์ตโฟนในสภาพแสงของ Data Center
- **สี Action หลัก:** ใช้สีน้ำเงินมั่นคง (`bg-blue-600 hover:bg-blue-500`) ปราศจากแสงนีออนสะท้อนแสงหรือเอฟเฟกต์สีรุ้ง
- **ปราศจากอิโมจิ (Zero Emoji Tropes):** ใช้ Technical Vector Icons จาก Lucide Icons และป้ายข้อความมาตรฐานวิศวกรรม
- **ตัวเลขแบบ Tabular Monospace:** ค่าทางไฟฟ้า อุณหภูมิ และเวลา จัดแสดงด้วยฟอนต์โมโนสเปซเพื่อให้อ่านง่ายและหลักตรงกันเสมอ

---

## 3. โครงสร้างบัญชีผู้ใช้และระดับสิทธิ์

| สิทธิ์ (Role) | สิทธิ์การเข้าถึงและการทำงาน |
|---|---|
| **ADMIN (ผู้ดูแลระบบ)** | อนุมัติ/ปฏิเสธคำขอลงทะเบียนของพนักงานใหม่, ปรับบทบาทสิทธิ์, เพิ่ม/ลบห้อง, เพิ่ม/ลบอุปกรณ์, พิมพ์ป้าย QR Code, ตรวจสอบ Dashboard และ Audit Log ทั้งหมด |
| **OPERATOR (เจ้าหน้าที่ตรวจ)** | เริ่มรอบเดินตรวจ, บันทึกค่าพารามิเตอร์อุปกรณ์, สแกน QR Code, แนบรูปจุดบกพร่อง (Defect), ตรวจสอบ Checklist และดูประวัติการเดินตรวจ |

### บัญชีทดสอบมาตรฐานในระบบ (Preset Accounts)
- **Admin:** Username: `admin` | Password: `admin123`
- **Operator:** Username: `technician1` | Password: `tech123`

*(หน้าจอเข้าสู่ระบบมีปุ่มลัดสำหรับคลิกกรอกบัญชีทดสอบทั้งสองให้อัตโนมัติ)*

---

## 4. ฟังก์ชันการทำงานหลัก

### 4.1 งานเดินตรวจรอบ (Routine Patrol & Inspection)
- กำหนดชื่อกะ/รอบตรวจ (เช่น *กะเช้า 08:00*, *กะบ่าย 16:00*, *กะดึก 00:00*)
- ฟอร์มบันทึกค่าจะปรับเปลี่ยนฟิลด์ที่ต้องกรอกโดยอัตโนมัติตามชนิดอุปกรณ์ (Dynamic Input Fields):
  - **ตู้ PDU (Power Distribution Unit):** แรงดันไฟ 3 เฟส, กระแสไฟ 3 เฟส, กำลังไฟฟ้ารวม (kW)
  - **ระบบ UPS (Uninterruptible Power Supply):** Input/Output Voltage, โหลดใช้งาน (% Load), แรงดันแบตเตอรี่ (Vdc)
  - **เครื่องปรับอากาศแม่นยำ CRAC/CRAH:** อุณหภูมิลมกลับ, อุณหภูมิลมจ่าย, ความชื้นสัมพัทธ์ (%RH), ความเร็วพัดลม (%)
- ระบบบันทึกภาพ Defect: ปิดเป็นค่าเริ่มต้น และจะแสดงช่องถ่ายรูปเมื่อเปิดสวิตช์ *"รายงานข้อบกพร่อง (Defect)"* เท่านั้น พร้อมการย่อขนาดภาพด้วย HTML5 Canvas (< 300 KB)

### 4.2 ระบบเลือกอุปกรณ์ด่วนและ Checklist รายห้อง
- **ดรอปดาวน์เลือกอุปกรณ์ด่วน (Direct Dropdown):** ช่างสามารถเลือกอุปกรณ์จากดรอปดาวน์ได้ทันทีโดยไม่ต้องเดินสแกนหา มีป้ายกำกับชัดเจน:
  - `[ตรวจแล้ว]` สำหรับอุปกรณ์ที่บันทึกค่าเรียบร้อย
  - `[รอตรวจ]` สำหรับอุปกรณ์ที่ยังไม่ได้บันทึก
- **ปุ่ม "ตรวจจุดถัดไป":** คลิกเดียวเพื่อสลับไปยังอุปกรณ์ที่ยังค้างอยู่ตัวถัดไป
- **Badge สถานะประจำห้อง:** แสดงจำนวนอุปกรณ์ที่ตรวจแล้วในห้องนั้นๆ (เช่น `ตรวจแล้ว (2/2)`, `ตรวจแล้ว (1/2)`, `ยังไม่ตรวจ (0/2)`)
- **Checklist สรุปรายการทั้งอาคาร:** เปิดหน้าต่างตรวจสอบว่าในแต่ละห้องเหลืออุปกรณ์ใดที่ยังไม่ตรวจ พร้อมปุ่ม *"ไปตรวจจุดนี้"* เพื่อกระโดดไปทำงานได้ทันที
- **ระบบป้องกันการตรวจไม่ครบ (Completion Guard):** หากกดปุ่ม *"เสร็จสิ้นรอบตรวจ"* ในขณะที่ยังมีอุปกรณ์ตกค้าง ระบบจะแสดงกล่องแจ้งเตือนรายชื่ออุปกรณ์ที่ยังขาดเพื่อให้ช่างตรวจสอบก่อนยืนยัน

### 4.3 แดชบอร์ดและบทวิเคราะห์พลังงาน (Energy Analytics)
- **Hotspot Detection (จุดความร้อนสะสม):** คัดกรองจุดที่มีอุณหภูมิเกิน 26°C (ตามเกณฑ์มาตรฐาน ASHRAE TC 9.9) เพื่อเตือนให้ตรวจสอบทางเดินลมและตำแหน่งปิด Blanking Panel
- **Overcooling Detection (จุดทำความเย็นเกินจำเป็น):** ตรวจจับจุดที่มีอุณหภูมิต่ำกว่า 19°C ซึ่งแสดงถึงโอกาสในการปรับเพิ่ม Setpoint แอร์ขึ้น เพื่อลดภาระเครื่องคอมเพรสเซอร์และประหยัดค่าไฟ
- **กราฟแนวโน้มอุณหภูมิและความชื้น (Dual-Axis Trends):** แสดงเส้นอุณหภูมิ (°C) คู่กับความชื้น (%RH) ตามลำดับเวลา
- **กราฟกำลังไฟฟ้า (Active Power Trends kW):** กราฟพื้นที่แสดงแนวโน้มการใช้ไฟฟ้ารวมในแต่ละรอบ
- **Defect Audit Gallery:** รายการภาพถ่ายความผิดปกติ พร้อมหมายเหตุ วันเวลา และฟังก์ชันคลิกดูภาพขยาย

### 4.4 ประวัติการเดินตรวจย้อนหลัง (Audit Log)
- ตารางบันทึกประวัติรอบการตรวจ ค้นหาตามวันเวลา และชื่อผู้ตรวจ
- คลิกดูรายละเอียดอุปกรณ์ทุกลำดับในรอบนั้นๆ พร้อมค่าตัวเลขและรูปภาพ

### 4.5 การจัดการอุปกรณ์ ห้อง และพิมพ์ป้าย QR Code
- **เพิ่ม/แก้ไขห้อง Data Center:** ระบุรหัสห้อง (เช่น `SR-01`, `UPS-01`), ชื่อห้อง, ชั้น และคำอธิบาย
- **เพิ่มอุปกรณ์พร้อมพรีเซ็ตสำเร็จรูป:** สามารถเลือกชื่อมาตรฐาน เช่น `PDU Server Hall Row A`, `CRAC Unit 01`, `UPS System A` หรือพิมพ์เองได้
- **เครื่องมือพิมพ์ป้าย QR Code (Asset Sticker):** ระบบสร้างป้าย Asset ขนาดกะทัดรัด คมชัด พร้อมชื่ออุปกรณ์ รหัสตู้ และ QR Code สำหรับสั่งพิมพ์ (Print) ไปติดหน้าตู้

### 4.6 การทำงานแบบออฟไลน์ (Offline Mode & Auto-Sync)
- ฐานข้อมูลบนเบราว์เซอร์ **Dexie.js (IndexedDB)** จัดเก็บรอบตรวจและค่าที่บันทึกไว้ในเครื่องทันที
- หากเกิดสภาวะอับสัญญาณ (Offline) ระบบจะแจ้งสถานะเป็นสีส้มและอนุญาตให้บันทึกต่อได้ตามปกติ
- เมื่อสัญญาณกลับมา ระบบจะทำการส่งข้อมูลที่ค้างอยู่ขึ้นเซิร์ฟเวอร์ให้อัตโนมัติ (Background Auto-Sync)

---

## 5. ขั้นตอนการปฏิบัติงานของเจ้าหน้าที่ (Standard Operating Procedure)

1. **เข้าสู่ระบบ:**
   - เข้าผ่านเว็บเบราว์เซอร์ `http://localhost:3000` (หรือ IP เซิร์ฟเวอร์)
   - กรอก Username และ Password ของตนเอง (กรณีพนักงานใหม่ ให้กด *"ลงทะเบียนขอสิทธิ์พนักงาน"* แล้วรอ Admin อนุมัติ)
2. **เริ่มรอบการเดินตรวจ:**
   - ไปที่เมนู **"เดินตรวจรอบ (Inspection)"**
   - พิมพ์ชื่อกะหรือเลือกรอบตรวจ แล้วกดปุ่ม *"เริ่มรอบตรวจ"*
3. **การบันทึกค่าอุปกรณ์:**
   - **วิธีที่ 1 (แนะนำ):** สแกนป้าย QR Code ที่ติดอยู่หน้าตู้ Rack / อุปกรณ์
   - **วิธีที่ 2:** เลือกชื่ออุปกรณ์จากดรอปดาวน์ *"เลือกอุปกรณ์"* หรือคลิกเลือกจากห้องในหน้าจอ
   - กรอกตัวเลขตามมาตรวัดหน้าตู้
   - หากพบจุดผิดปกติ ให้เปิดสวิตช์ *"รายงานข้อบกพร่อง (Defect)"* พิมพ์รายละเอียด และถ่ายรูปแนบ
   - กดปุ่ม *"บันทึกข้อมูลอุปกรณ์นี้"*
   - กดปุ่ม *"ตรวจจุดถัดไป"* เพื่อข้ามไปอุปกรณ์ถัดไปอย่างรวดเร็ว
4. **ตรวจสอบความเรียบร้อยก่อนปิดรอบ:**
   - กดปุ่ม *"ตรวจสอบรายการที่เหลือ"* เพื่อดูว่าอุปกรณ์ในทุกห้องตรวจครบถ้วนแล้วหรือไม่
   - หากครบถ้วนแล้ว ให้กดปุ่ม *"เสร็จสิ้นรอบตรวจ (Complete Round)"*

---

## 6. ขั้นตอนการทำงานของผู้ดูแลระบบ (Admin Operation)

1. **การอนุมัติผู้ใช้งานใหม่:**
   - ไปที่เมนู **"อนุมัติผู้ใช้ (User Approval)"**
   - ตรวจสอบรายชื่อในกล่อง *"รายการที่รอการอนุมัติ"*
   - กดปุ่ม **"อนุมัติ (Operator)"** เพื่อเปิดให้พนักงานเริ่มทำงานได้ทันที หรือแต่งตั้งเป็น **Admin**
2. **การจัดการห้องและอุปกรณ์:**
   - ไปที่เมนู **"จัดการอุปกรณ์ (Equipment)"**
   - กดปุ่ม *"เพิ่มห้องใหม่"* หากมีโซนหรือห้องเพิ่มเติม
   - กดปุ่ม *"เพิ่มอุปกรณ์ใหม่"* โดยสามารถเลือกจากพรีเซ็ตมาตรฐานเพื่อความรวดเร็ว
3. **การพิมพ์ป้าย QR Code:**
   - ในตารางอุปกรณ์ กดปุ่ม **"พิมพ์ QR"**
   - ระบบจะเปิดกล่องป้ายสติกเกอร์ขึ้นมา ให้กดปุ่ม *"พิมพ์ป้ายสติกเกอร์ (Print)"* เพื่อสั่งพิมพ์ออกทางเครื่องพิมพ์สติกเกอร์
4. **การวิเคราะห์แดชบอร์ด:**
   - ตรวจสอบแถบเตือน **Hotspot** และ **Overcooling** เพื่อวิเคราะห์การตั้งค่าอุณหภูมิและการจัดทางเดินลมเย็นใน Data Center

---

## 7. โครงสร้างทางเทคนิคและสถาปัตยกรรมโปรเจกต์

```
d:\WebApp\Operation Service\
├── backend/
│   ├── prisma/
│   │   ├── schema.prisma          # โครงสร้างฐานข้อมูล SQLite/PostgreSQL
│   │   └── seed.ts                # สคริปต์เพิ่มข้อมูลตัวอย่าง (ห้อง, อุปกรณ์, ข้อมูลย้อนหลัง)
│   ├── src/
│   │   ├── controllers/           # ตรรกะประมวลผล Auth, Room, Equipment, Inspection, Dashboard
│   │   ├── middleware/            # ตรวจสอบ JWT Token และสิทธิ์ Admin
│   │   ├── routes/                # กำหนดเส้นทาง REST API
│   │   └── server.ts              # Express Application Server
│   ├── uploads/                   # โฟลเดอร์จัดเก็บไฟล์ภาพถ่าย Defect
│   └── package.json
├── frontend/
│   ├── src/
│   │   ├── components/            # Navbar, QRScannerModal, ProtectedRoute
│   │   ├── context/               # AuthContext (จัดการการเข้าสู่ระบบและ Token)
│   │   ├── pages/                 # Login, Register, Dashboard, Inspection, History, Admin Pages
│   │   ├── services/              # api.ts (Axios), offlineDb.ts (IndexedDB)
│   │   ├── types/                 # TypeScript Interfaces
│   │   ├── index.css              # Custom Styling & Tabular Numbers
│   │   └── App.tsx                # App Routing & Navigation
│   ├── index.html                 # Main Entrypoint
│   └── package.json
├── start-dc-ops.bat               # สคริปต์คลิกเดียวสำหรับเปิดระบบบน Windows
├── README.md                      # สรุปภาพรวมโปรเจกต์
└── SYSTEM_MANUAL.md               # คู่มือการใช้งานฉบับสมบูรณ์ (ไฟล์นี้)
```

---

## 8. รายการ API Endpoints

### 8.1 ระบบยืนยันตัวตน (Authentication)
- `POST /api/auth/register` - ลงทะเบียนผู้ใช้ใหม่ (สถานะ PENDING)
- `POST /api/auth/login` - เข้าสู่ระบบ (รับ JWT Token)
- `GET /api/auth/me` - ตรวจสอบข้อมูลผู้ใช้ปัจจุบัน
- `GET /api/auth/users` - ดึงรายชื่อผู้ใช้ทั้งหมด (Admin Only)
- `PUT /api/auth/users/:id/status` - อนุมัติ/ปฏิเสธ หรือเปลี่ยน Role ผู้ใช้ (Admin Only)

### 8.2 ทรัพยากรห้องและอุปกรณ์ (Rooms & Equipment)
- `GET /api/rooms` - ดึงรายชื่อห้องทั้งหมด
- `POST /api/rooms` - เพิ่มห้องใหม่ (Admin Only)
- `GET /api/equipments` - ดึงรายชื่ออุปกรณ์ทั้งหมด
- `POST /api/equipments` - เพิ่มอุปกรณ์ใหม่ (Admin Only)
- `DELETE /api/equipments/:id` - ลบอุปกรณ์ (Admin Only)
- `GET /api/equipments/types` - ดึงประเภทอุปกรณ์และรูปแบบฟิลด์ (PDU, CRAC, UPS)
- `GET /api/equipments/qr/:qrCode` - ค้นหาอุปกรณ์ด้วยรหัส QR Code

### 8.3 การเดินตรวจ (Inspections)
- `POST /api/inspections/start` - เปิดรอบการเดินตรวจใหม่
- `GET /api/inspections/active` - ตรวจสอบรอบตรวจที่กำลังดำเนินการอยู่
- `POST /api/inspections/log` - บันทึกค่าอุปกรณ์รายตัว
- `POST /api/inspections/sync` - ซิงก์ข้อมูลเป็นชุดสำหรับโหมดออฟไลน์
- `POST /api/inspections/:id/complete` - ปิดรอบการตรวจ
- `GET /api/inspections/history` - ดึงประวัติรอบการตรวจย้อนหลัง
- `GET /api/inspections/rounds/:id` - ดึงรายละเอียดข้อมูลในรอบตรวจที่เลือก

### 8.4 แดชบอร์ดและไฟล์แนบ (Dashboard & Upload)
- `GET /api/dashboard/summary` - สรุปสถานะภาพรวม, จุด Hotspot, จุด Overcooling
- `GET /api/dashboard/temp-humidity-trends` - ข้อมูลแนวโน้มอุณหภูมิและความชื้น
- `GET /api/dashboard/power-trends` - ข้อมูลแนวโน้มกำลังไฟฟ้า (kW)
- `GET /api/dashboard/defects` - รายการภาพถ่ายและข้อบกพร่องล่าสุด
- `POST /api/upload/photo` - อัปโหลดรูปภาพ Defect (รองรับ multipart/form-data)

---

## 9. การเปิดใช้งานระบบและการติดตั้ง

### 9.1 รันอัตโนมัติบน Windows
ดับเบิลคลิกไฟล์ **`start-dc-ops.bat`** ในโฟลเดอร์ `d:\WebApp\Operation Service`
ระบบจะเปิดหน้าต่างรัน Backend (Port 5000) และ Frontend (Port 3000) ให้อัตโนมัติ

### 9.2 รันผ่าน Terminal / Command Line
1. **เปิด Backend Service:**
   ```bash
   cd "d:\WebApp\Operation Service\backend"
   npm.cmd run dev
   ```
2. **เปิด Frontend Service:**
   ```bash
   cd "d:\WebApp\Operation Service\frontend"
   npm.cmd run dev
   ```
3. **การเข้าใช้งานผ่านเบราว์เซอร์:**
   - เข้าทางคอมพิวเตอร์เครื่องหลัก: `http://localhost:3000`
   - เข้าทางมือถือ/แท็บเล็ตในวงแลนเดียวกัน: `http://<IP-เครื่องเซิร์ฟเวอร์>:3000` (เช่น `http://192.168.1.50:3000`)
